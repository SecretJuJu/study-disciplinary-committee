/** @type {import("prettier").Config} */
const config = {
  arrowParens: 'always',
  printWidth: 100,
  quoteProps: 'consistent',
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
};

export default config;
